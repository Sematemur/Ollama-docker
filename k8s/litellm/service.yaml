apiVersion: v1
kind: Service
metadata:
  name: litellm-service
  namespace: chat-system
  labels:
    app: litellm
    app.kubernetes.io/part-of: chat-system
spec:
  type: ClusterIP
  selector:
    app: litellm
  ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
