apiVersion: v1
kind: Service
metadata:
  name: loki-service
  namespace: chat-system
  labels:
    app: loki
    app.kubernetes.io/part-of: chat-system
spec:
  type: ClusterIP
  selector:
    app: loki
  ports:
    - port: 3100
      targetPort: 3100
      protocol: TCP
      name: http
