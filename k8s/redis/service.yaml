apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: chat-system
  labels:
    app: redis
    app.kubernetes.io/part-of: chat-system
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
